[phases.setup]
nixPkgs = ["python311Full", "pkg-config", "lapack", "blas"]

[phases.build]
cmds = ["python3.11 -m pip install --no-cache-dir -r requirements_raw_recording.txt"]

[start]
cmd = "bash start.sh"






