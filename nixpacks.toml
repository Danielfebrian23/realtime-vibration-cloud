providers = ["python"]

[phases.setup]
nixPkgs = ["python312Full", "python312Packages.pip", "python312Packages.setuptools", "python312Packages.wheel"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements_raw_recording.txt"
]

[start]
cmd = "python3 realtime_vibration_server_raw_recording.py"












