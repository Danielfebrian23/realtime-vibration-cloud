providers = ["python"]

[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "pkg-config", "lapack", "blas"]

[phases.install]
cmds = ["python3 -m pip install --no-cache-dir -r requirements_raw_recording.txt"]

[start]
cmd = "python3 realtime_vibration_server_raw_recording.py"







