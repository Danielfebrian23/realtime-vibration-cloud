[phases.setup]
nixPkgs = [
  "python311",
  "pkg-config",
  "lapack",
  "blas"
]

[phases.build]
cmds = ["pip install --no-cache-dir -r requirements_raw_recording.txt"]

[start]
cmd = "python3 realtime_vibration_server_raw_recording.py"




